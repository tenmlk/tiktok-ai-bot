name: Telegram Bot Poll

on:
  schedule:
    # Run every 5 minutes (most frequent allowed on GitHub Actions free)
    # NOTE: GitHub Actions cron can be delayed 5-30 min during high load.
    - cron: '*/5 * * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: read

concurrency:
  group: bot-poll
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd username-bot
          npm install --no-audit --no-fund --omit=dev

      - name: Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          ZAI_CONFIG: ${{ secrets.ZAI_CONFIG }}
        run: |
          cd username-bot
          node bot-once.js
